<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>Label</key>
    <string>eu.flawn.instantspaces.inject</string>

    <key>ProgramArguments</key>
    <array>
      <string>/Users/flawn/Documents/Projects/instantspaces/scripts/auto-inject.sh</string>
      <string>min0125</string>
    </array>

    <!-- [AI] Run when loaded (login) and only keep alive on failure (so success exits cleanly) -->
    <key>RunAtLoad</key><true/>
    <key>KeepAlive</key>
    <dict>
      <key>SuccessfulExit</key>
      <false/>
    </dict>

    <!-- Ensure login session -->
    <key>LimitLoadToSessionType</key>
    <string>Aqua</string>

    <!-- Environment PATH for lldb -->
    <key>EnvironmentVariables</key>
    <dict>
      <key>PATH</key>
      <string>/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>

    <!-- Optional: throttle restarts in case of rapid failures -->
    <key>ThrottleInterval</key>
    <integer>5</integer>

    <!-- [AI] Optional: log paths for debugging -->
    <key>StandardOutPath</key>
    <string>/Users/flawn/Library/Logs/instantspaces.auto-inject.out.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/flawn/Library/Logs/instantspaces.auto-inject.err.log</string>
  </dict>
</plist>